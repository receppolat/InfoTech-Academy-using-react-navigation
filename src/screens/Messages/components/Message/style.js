import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    image:{
        width: 65,
        height: 65,
        borderRadius: 50,
        margin: '2%'
    }
})
export default styles;