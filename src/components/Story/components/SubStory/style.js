import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        width: 85,
        height: 85,
        borderRadius: 50,
        borderWidth: 2.4,
        borderColor: 'red',
        marginRight: 10,
        marginLeft: 3
    }
})

export default styles;