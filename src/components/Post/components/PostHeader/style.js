import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    container: {
        marginTop: '2%',
        flexDirection: 'row',
        alignItems: 'center'
    },
    userImage: {
        width: 35,
        height: 35,
        borderRadius: 50,
        marginRight: '2%'
    }
})

export default styles;